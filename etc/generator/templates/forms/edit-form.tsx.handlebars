import React from 'react'
import { {{name}}Interface } from '../../models/{{name}}Interface'
import { translate, TranslateProps } from "react-polyglot"

export interface {{name}}EntityEditFormPropsInterface {
  entity: {{name}}Interface|null
}

export interface {{name}}EntityEditFormStateInterface {

}

export class {{name}}EntityEditForm extends React.Component<{{name}}EntityEditFormPropsInterface, {{name}}EntityEditFormStateInterface> {
  render() {
    return ''
  }
}

export default translate()({{name}}EntityEditForm)
