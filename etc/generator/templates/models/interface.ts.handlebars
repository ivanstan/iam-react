import { CollectionInterface } from './CollectionInterface'
{{#if hasAssociations }}
{{#each fields}}
{{#if this.target}}
import { {{this.target}}Interface } from './{{this.target}}Interface'
{{/if}}
{{/each}}

{{/if}}
export interface {{name}}Interface {
{{#each fields}}
    {{@key}}: {{this.typescript}}
{{/each}}
}

export interface {{name}}CollectionInterface extends CollectionInterface {
  members: {{name}}Interface[]
}
